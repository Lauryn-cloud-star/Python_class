# = is an assignmeent operator because it tells the 
# 100 is an operand
# an opperator is a character or symbol that tells the computer what to do with an operand or value
# types of operstors in python include:
# arithmetic operators eg +, -, *, /, %, **, //
# comparison operators eg ==, !=, <, >, <=, >=
# logical operators eg and, or, not
# assignment operators eg =, +=, -=, *=, /=, %=, **=, //=
# bitwise operators eg &, |, ^, ~, <<, >>
# special operators eg is, is not, in, not in

num = 100